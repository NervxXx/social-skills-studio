# PostgreSQL for SocialSim backend
services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: socialsim
      POSTGRES_PASSWORD: socialsim435323324242
      POSTGRES_DB: socialsim
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
